<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/error_frame"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="top"
    android:layout_marginVertical="@dimen/item_margin_vertical"
    android:orientation="vertical">

    <TextView
        android:id="@+id/header"
        style="@style/HeaderTextStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_medium"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="@string/example_word" />

    <TextView
        android:id="@+id/description"
        style="@style/BaseTextStyle"
        android:layout_width="@dimen/zero"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/margin_medium"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/header"
        tools:text="@string/example_translate" />

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/favourite"
        android:layout_width="@dimen/favourite_icon_size"
        android:layout_height="@dimen/favourite_icon_size"
        android:layout_margin="@dimen/favourite_icon_margin"
        android:alpha="@dimen/favourite_icon_alpha"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_favourite_no" />
</androidx.constraintlayout.widget.ConstraintLayout>